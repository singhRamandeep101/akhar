# Audio clips

MP3s are generated by `npm run generate-audio` (Azure `pa-IN-VaaniNeural`).

Expected layout:

- `letters/*.mp3` — letter names (e.g. ਊੜਾ)
- `matras/*.mp3` — matra forms on ਕ
- `words/*.mp3` — vocabulary

Until you run the generator, Play sound shows “Audio missing”.
